import React from 'react';
import {StyleSheet, View} from 'react-native';

const Reels = () => {
  return <View style={styles.position} />;
};

const styles = StyleSheet.create({position: {position: 'relative'}});

export default Reels;
